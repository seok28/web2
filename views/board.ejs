<!DOCTYPE html>
<html lang="ko">
<head>
    <%- include('./head') %>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('./nav') %>
    <div class="container mb-3">

        <h2 class="mb-3">게시판</h2>
  
        <table class="board-table table table-sm border-bottom">
  
          <thead class="thead-light">
            <tr>
              <th scope="col">제목</th>
              <th scope="col" class="date">작성자 </th>
            </tr>
          </thead>
  
          <tbody>
            <% if(posts == null || posts.length == 0){ %>
              <tr>
                <td colspan=2> 입력된 데이터가 없습니다.  </td>
              </tr>
            <% } %>
            <% posts.forEach(function(post) { %>
              <tr>
                <td>
                  <a href="/board/<%= post.id %>"><div class="ellipsis"><%= post.title %></div></a>
                </td>
                <td class="date">
                  <span data-date> <%= post.id %> </span>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
        <div>
          <a class="btn btn-primary" href="/board/write">등록</a>
        </div>
      </div>
    </body>
    
  </html>